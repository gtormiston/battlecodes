var myCodeMirror = CodeMirror.fromTextArea(document.getElementById("solution"), {
  lineNumbers: true,
  matchBrackets: true,
  mode: 'javascript'
});
// myCodeMirror();
// var textArea = document.getElementById("codeMirror");
// var editor = CodeMirror.fromTextArea(textArea);
